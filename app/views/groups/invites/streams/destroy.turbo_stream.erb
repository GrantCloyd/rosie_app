<%= turbo_stream.remove "invite-#{@invite.id}" %>

<%= turbo_stream.replace "flash"  do %>
  <% flash[:alert] = "Invite for #{@invite.target_email} has been deleted. Note - email has already been sent" %>
  <%= render 'layouts/flash' %>
<% end %>