<%= turbo_stream.remove "invite-#{@invite.id}" %>

<%= turbo_stream.append "groups-container" do %>
  <%= render 'groups/partials/group', group: @group, user_group: @user_group %>
<% end%>

<%= turbo_stream.update "flash" do %>
  <%= flash[:alert] = "Welcome to #{@group.title}!" %>
<% end %>