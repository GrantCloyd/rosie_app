<%= turbo_frame_tag "main_view" do  %>
  <div class="container-md">
  <h1 class='<%="#{primary_header}"%>'> Create New Post for "<%= @section.title %>"</h1>

  <%= render "groups/sections/posts/partials/posts-buttons", view: :new, cached: true %>

  <%= render "groups/sections/posts/partials/form", group: @group, section: @section, post: Post.new, view: :new %>

  </div>
<% end %>