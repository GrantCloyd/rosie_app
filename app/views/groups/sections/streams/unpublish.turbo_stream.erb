<%= turbo_stream.remove "section_#{@section.id}" %>

<%= turbo_stream.append "sections-unpublished-container" do %>
  <%= render partial: 'groups/partials/section', 
  locals: {
    group: @group, 
    section: @section, 
    user_group_section: @user_group_section, 
    view: :unpublished
    } %>
<% end %>

<%= turbo_stream.replace "flash"  do %>
  <% flash[:alert] = "#{@section.title} has been hidden!" %>
  <%= render 'layouts/flash' %>
<% end %>