<%= turbo_stream.remove "image-#{@image.blob_id}" %>

<%= turbo_stream.remove "hidden-image-#{@image.blob_id}" %>

<%= turbo_stream.replace "flash"  do %>
  <% flash[:alert] = "Image removed from post" %>
  <%= render 'layouts/flash' %>
<% end%>