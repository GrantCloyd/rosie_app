<% if view != :new %>
  <button class='<%="#{primary_button}"%>'>
    <%= link_to "Create New Topic", new_topic_path, data: {turbo_action: :advance} %>
  </button>
<% end%>

<% if view != :index %>
  <button class='<%="#{primary_button}"%>'>
    <%= link_to "Topics", topics_path, data: {turbo_action: :advance} %>
  </button>
<% end%>
