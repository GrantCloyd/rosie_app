<%= turbo_frame_tag "main_view" do  %>
<div class="container-md">
  <h1 class='<%="#{primary_header}"%>'> Create New Topic </h1>

  <%= render "topics-buttons", view: :new, cached: true %>

  <%= render "form", topic: @section.topics.new , section: @section  %>

</div>
<% end %>